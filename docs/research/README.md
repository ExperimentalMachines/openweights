# Research notes

Every measured report and decision record in this directory, with what it asks and what
it found. Dates are the last substantive change. Three reports also have published pages:
[latency](https://experimentalmachines.github.io/openweights/latency.html),
[the exported-window study](https://experimentalmachines.github.io/openweights/window.html) and
[its reruns](https://experimentalmachines.github.io/openweights/reruns.html).

## Measured reports

| Note | Date | What it asks | What it found |
|---|---|---|---|
| [npu-pd-disaggregation.md](npu-pd-disaggregation.md) | 2026-09-22 | Can the NeuroPilot NPU read the prompt and the CPU write the reply, inside the app rather than from a shell | It works and stays off on cost. An app cannot load MediaTek's adapter without naming it in `uses-native-library`, which is why every build crashed at launch; with that fixed the path answers correctly on four prompts and the handoff costs under a millisecond. Two things stop it paying: the export holds 512 tokens against the app's 2,050, and this runner's decode is 13 to 18 tok/s against the app's own 27 on the same file. CPU decode beats NPU decode, so the split is the right way round; the prefill win is 1.7x against the app's real CPU path. Five defects fixed on the way, and an earlier verdict of mine corrected: the handoff was never the problem, a chat-template-free test prompt was |
| [executorch-state-and-recipes.md](executorch-state-and-recipes.md) | 2026-09-21 | What recurrent state and quantization changed in historical compiled LFM2.5 experiments | Clearing short-convolution state and solving int4 codes recovered many tool calls at the same format and size, with prompt-dependent recall and unnecessary-search tradeoffs. The historical builds are not interchangeable with current Hub files. Model-side evidence is archived under `~/ow-models/benchmarks/`; approval of the later captured published GPTQ32k artifact remains withheld after correctness, compaction and tool-outcome failures. |
| [typesafe-experiments.md](typesafe-experiments.md) | 2026-09-17 | Where the loop's intent patterns and the eval grader fail, read against TypeSafe's hosted Jev on 2,759 recorded no-call replies and 1,997 searched rows (workstation only) | The patterns act only where Jev agrees and on 36% of what it flags, and what they miss is wrong 89% of the time; two lament shapes cover 72 replies with 2 right answers, to be built as a search-only predicate; the grader credited 251 of 8,500 rows through in-word alias matches ("CA" in "located"), now word-bounded, with 5 of 76 paired comparisons changing direction, all near ties; replies are right 43% when the results state the answer and 7% when not; on the Poco, reading the top page when the model says the snippets fall short took 60 correct to 66 (paired 6 to 0) at 15 more seconds on the rows that read, with a cutoff tuned on those rows |
| [judged-decisions.md](judged-decisions.md) | 2026-09-17 | Whether the loop can ask the model closed questions (search first? which tool? is this news? does this follow on?) and read the answers as probabilities instead of parsing prose | The engine asks with one-token options and a mass floor, and on LFM2.5 a judged turn replies token for token as a clean one while reading 5 tokens (Qwen3.5 2B diverges with where a read is split, judgement or not); offline the "are you sure" gate ranks wrong answers at AUROC 0.62/0.61 against the doubt gate's 0.76/0.69 on LFM2.5, yet what it sends is six wrong answers a split the doubt gate misses, and on Qwen3.5 2B it ranks level (0.68 against 0.70) with a cutoff that does not transfer; every judgement ships off, with phone arms owed |
| [dropped-pass-reread.md](dropped-pass-reread.md) | 2026-09-14 | Why a GGUF reply that the loop searched for was slow with the warm prefix loaded, while the compiled model was not | The search the loop makes for the model replaced the model's pass, and a hybrid cache cannot cut back to it, so it re-read everything after the warm prefix; the engine now keeps llama.cpp's partial-state checkpoint one token before each prompt's end and restores it: 866 tokens read become 323 on a Pixel, a regenerate reads 1 and replies byte for byte the same |
| [release-token-callback-abort.md](release-token-callback-abort.md) | 2026-09-14 | Why the Play build closed on "hi" to Liquid's QAD-Q4_0 GGUF | Every GGUF reply in every release from version code 604 aborted: the token callback gained an argument, the R8 keep rule still named the old signature, and the name-only guard passed; rules now match by name, native code clears the pending exception, the build checks signatures against the dex, and a unit test ties the C++, Kotlin and rules together; verified on the Poco |
| [executorch-tool-calling-qad.md](executorch-tool-calling-qad.md) | 2026-09-13 | Can an ExecuTorch export of LFM2.5 1.2B be made to call tools: the quantisation-aware weights, the exact Q4_0 grid, and int4 on one module family at a time | The bf16 checkpoint goes to int4 groups of 32 with int8 activations; QAD weights recover half the calls and their grid is unreachable on XNNPACK; int4 on attention is harmless and int4 on the feed-forward alone kills the call, so every calling recipe keeps the feed-forward at int8 and is 1.1 to 1.2 GB against the GGUF's 697 MB; the fp32 export's own gap is not the SDPA op; next is activation-aware scaling of the feed-forward, then QAT |
| [executorch-tool-calling.md](executorch-tool-calling.md) | 2026-09-13 | Why the compiled LFM2.5 1.2B writes "Based on my search" and calls nothing while the GGUF calls, reproduced on a laptop with first-token probabilities on the phone's own prompt bytes | Two thirds of the 2026-09-10 gap was the app's named-subject note obeyed by the GGUF on 42 of 42 rows and by the export on none; on identical bytes the shipped 8da4w export puts under 0.01 on the tool-call token where the GGUF and an unquantised ExecuTorch export put 0.8, the loss compounds across the body rather than sitting in the LM head, and no replacement recipe is named; two app defects fixed on the way (the tool schema spelled on many lines cost 0.36 of the mass on the GGUF; warm pieces cut after a space changed tokens) |
| [recommended-runtime.md](recommended-runtime.md) | 2026-09-10 | Which runtime the shortlist should recommend for LFM2.5, read from every table on file rather than one row | The 8da4w exports score 13.0 against 19.6 (GSM8K), 15.6 against 21.6 (IFEval) and 22.4 against 26.0 (BFCL) of 30 over five phones, call web search on 1 to 4 of 75 rows that need it against the GGUF's 33, fabricate a search in one reply of five against none, diverge between phones at 31.5% against 13.3%, and return no logits; the shortlist's LFM2.5 rows are GGUF again, the compiled pair stay as the modified rows, and nothing else was reverted |
| [retrieve-or-answer.md](retrieve-or-answer.md) | 2026-09-10 | Whether a small model on a phone searches when it must, measured on 160 public rows through the real loop on four phones, and what in the loop moves that without taking the decision from it | The compiled LFM2.5 1.2B searched 3 of 75 rows that needed it and wrote "based on my search" without one in 32 of 160; the full catalogue over-calls at 12 s of first-token latency; the loop now carries out the search the model says it will make, says it made, or says it cannot make; the tail it answers confidently and wrongly is documented, not hidden |
| [who-is-questions.md](who-is-questions.md) | 2026-09-10 | Why "who is X" came back as "Let me look that up" with no search, what shipped, and why it was rejected the same day | The compiled LFM2.5 1.2B never calls web_search under the app's instructions (0 of 96 rows); a search-the-name-first route and a "concise" framing measured well on sixteen hand-written questions and were rejected: one-sentence replies, instructions quoted back on an unnamed question, and the decision taken from the model; two reviewers sent it to the standard-set harness below |
| [routing-literature.md](routing-literature.md) | 2026-09-10 | What the field has established about a small model deciding to search, and which benchmarks a phone can run | Thirty-six sources: RetrievalQA carries exactly the app's label; When2Call and When2Tool show the decision is in the model and the measured bias at this size is over-calling; "Search, Do not Guess" says small models should always search on multi-hop QA; tool count degrades selection, the opposite of the fresh-install hypothesis; conciseness instructions cut small models' detail |
| [executorch-window-matrix.md](executorch-window-matrix.md), tables in [window-matrix.md](window-matrix.md) | 2026-09-07 | Does the context window an ExecuTorch model is exported with change answers, tool calls, speed or memory? Four chips, five windows, 90 prompts | It changes memory at load and nothing else distinguishable from run-to-run variation; a full-attention model at 32k is killed by a 6 GB rule on the 12 GB phones tested |
| [executorch-own-exports.md](executorch-own-exports.md) | 2026-09-07 | Can we export LFM2.5 ourselves at 16k and 32k, and what does it cost? | A 1.2B export takes two minutes on a laptop; the window costs memory, not speed |
| [executorch-vision.md](executorch-vision.md) | 2026-09-07 | What it takes to feed a compiled vision export a picture | LFM2.5-VL encodes a 512 square in 1.5 s and decodes at 80 to 85 tok/s on a Dimensity 9400; two families are fed, two are refused with a reason |
| [vision-encoder-cost.md](vision-encoder-cost.md) | 2026-09-06 | Where a 44-second image turn goes | The vision tower is 35 to 43 s of it; the language model is a few seconds |
| [image-tokens.md](image-tokens.md) | 2026-09-05 | Is an image-token slider the fix for slow image turns? | No: fewer image tokens made the turn four times slower, because a lower budget turns tiling on |
| [public-benchmarks.md](public-benchmarks.md), tables in [benchmark-matrix.md](benchmark-matrix.md) | 2026-09-04 | GSM8K, IFEval and BFCL on both runtimes, six phones, same prompts and graders | llama.cpp ahead on grades everywhere; the scores are the leaderboard's rules applied to what the app's parser returned |
| [first-turn-latency.md](first-turn-latency.md) | 2026-09-04 | Where a 25.6-second cold first turn went | 18.5 s of it was before the first token; warming the prefix took a fresh chat under a second |
| [tool-calling.md](tool-calling.md) | 2026-09-04 | What moves tool-choice accuracy on a 1B model | Which model it is matters more than the prompt or the route |
| [parity-five-socs.md](parity-five-socs.md), tables in [backend-parity.md](backend-parity.md) | 2026-09-03 | Do the two runtimes grade the same agentic prompts the same way across silicon? | llama.cpp graded identically on all five phones; ExecuTorch showed three cross-silicon divergences |
| [production-readiness.md](production-readiness.md) | 2026-09-03 | Closing every open question from the second-runtime work | Every llama.cpp grade identical across SoCs, ten rows case for case |
| [executorch-families.md](executorch-families.md) | 2026-08-31 | What supporting a `.pte` family costs | Four artifacts plus fixtures, about an afternoon, nothing in the engine changes |
| [kv-cache-regression.md](kv-cache-regression.md) | 2026-08-31 | A reported throughput regression | The prompt was being rewritten mid-conversation; the engines were unchanged |
| [gpu-backends.md](gpu-backends.md) | 2026-08-31 | Is the GPU worth using on the development phone? | The OpenCL driver works but ggml drops the Mali GPU as unsupported; the app runs on the CPU |
| [mediatek-npu.md](mediatek-npu.md) | 2026-08-30 | Should the app target the MediaTek NPU? | No: level at decode, 1.3x to 2.1x at prefill against a KleidiAI CPU |
| [npu-prefill-multiturn.md](npu-prefill-multiturn.md) | 2026-08-30 | Would NPU prefill pay on real conversations? | After cache reuse a turn prefills a median of 50 tokens; there is almost nothing to offload |
| [speculative-decoding.md](speculative-decoding.md) | 2026-08-30 | Does a draft model help decode on a phone? | 1.5x to 3x slower than plain decode |

## Decision records and design notes

| Note | Date | What it decides |
|---|---|---|
| [inference-engines.md](inference-engines.md) | 2026-08-24 | llama.cpp with GGUF as the engine for arbitrary Hub models |
| [executorch.md](executorch.md) | 2026-09-01 | ExecuTorch as the second runtime, XNNPACK first |
| [multimodality.md](multimodality.md) | 2026-08-24 | Which modalities go in and out: text, image, audio, sampled video in; text and Android TTS out |
| [web-search.md](web-search.md) | 2026-09-01 | Where web search comes from; DuckDuckGo, Brave and Yahoo, no self-hosted SearXNG |
| [memory-recall.md](memory-recall.md) | 2026-09-05 | A linear scan over a small memory rather than a vector index |
| [engine-settings.md](engine-settings.md) | 2026-09-05 | Which of a competitor's nineteen settings belong on the sheet: none |
| [capabilities-ia.md](capabilities-ia.md) | 2026-08-10 | Tools, skills and MCP are three different things on this platform |
| [loops-and-kv-cache.md](loops-and-kv-cache.md) | 2026-09-10 | Where the four agent loops live in the app; the canvas census (sixteen pages, no load-time error, three cut-off or stub pages) and the grader it shaped; and the KV formula corrected: Qwen3's stated key length is not the embedding over the heads, which had halved the 0.6B's cache estimate |
| [plan-mode-and-recall.md](plan-mode-and-recall.md) | 2026-09-05 | Plan mode must plan, not answer; recall and search credulity fixes |
| [date-in-the-prompt.md](date-in-the-prompt.md) | 2026-09-05 | Why a greeting got answered about the date |
| [generation-runtimes.md](generation-runtimes.md) | 2026-09-01 | No runtime publishes a reproducible on-phone image-generation measurement; not offered |
| [ios-strategy.md](ios-strategy.md) | 2026-09-01 | Native Swift over the shared C++ core for a second platform |
| [competitive-analysis.md](competitive-analysis.md) | 2026-08-31 | OpenWeights against seven cloud assistants and seven on-device apps |

## Review sweeps

| Note | Date | What it covers |
|---|---|---|
| [qa-sweep-2026-09-02.md](qa-sweep-2026-09-02.md) | 2026-09-03 | Six independent reviews over the whole app, every finding verified against the code |
| [gemini-review-2026-09-03.md](gemini-review-2026-09-03.md) | 2026-09-03 | An adversarial whole-codebase review: 65 claims, 43 fixed, 16 wrong, 6 left by choice |
| [repo-audit-2026-09-17.md](repo-audit-2026-09-17.md) | 2026-09-17 | The documents against the code and tools, read cold with the ExecuTorch path in front: what is consistent, what is stale, what a newcomer cannot reproduce, what was fixed the same day |
| [qa-sweep-2026-09-05.md](qa-sweep-2026-09-05.md) | 2026-09-05 | Commands, compiled models, canvas and page reading on a Snapdragon 8 Gen 3 |
